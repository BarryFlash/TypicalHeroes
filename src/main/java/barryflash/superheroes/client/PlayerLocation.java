package barryflash.superheroes.client;

import net.minecraft.entity.Entity;
import net.minecraft.entity.player.EntityPlayer;
import net.minecraft.util.math.BlockPos;
import net.minecraftforge.event.entity.player.PlayerEvent;

/*public class PlayerLocation extends PlayerEvent{
private final BlockPos playerlocation;
	
	public PlayerLocation(EntityPlayer player, BlockPos playerlocation) {
		super(player);
		this.playerlocation = playerlocation;
		
	}
	 public BlockPos getPlayerLocation()
	    {
	        return playerlocation;
	    }
	 
	 public static boolean PlayerRadius(EntityPlayer player, BlockPos playerlocation, Entity entity, float x, float y, float z) {
		return
		 
	 }
} */
